import React from 'react';
import Menu from '../Menu/Menu';
import styles from './Header.module.scss';

function Header() {
  return (
    <div className={styles.container}>
      <h1>Hi I'm Charlie, a web dev</h1>
    </div>
  );
}

export default Header;
